-PROJECT mới
  Lên git tạo repository  
  vào folder code
  git init
  git remote add origin <link remote>
  git add .
  git commit -m "noi dung can thong bao"
  git push origin master

-Project đã push lên git r cần update thêm code
  git add .
  git commit -m "noi dung can thong bao"
  git push origin master

-Lấy project trên github
 git clone https <link duong dan den project tren git>

- Các lệnh git thường dùng
  git branch  // kiểm tra branch hiện tại
  git branch <name_branch> // tạo mới 1 branch
  git checkout -b <name_branch> // Để chuyển và tạo mới
  git checkout <name_branch> // Checkout 1 nhánh

- Merge nhánh
  git checkout master
  git merge <branch cần merge>
